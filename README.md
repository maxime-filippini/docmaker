# Docmaker

Illustrative example of a document builder pipeline, based on Phoenix components,
ChromicPDF, and GenStage.

## To do

- [x] Build layouts for sample documents
- [x] Use ChromicPDF to build PDFs from HTML documents
- [x] Incorporate graphs into generated documents
- [x] Incorporate tables into generated documents
- [ ] Generate PDFs from in-memory HTML strings instead of files
- [ ] Build a pipeline using GenStage to illustrate how we could make this a real service
